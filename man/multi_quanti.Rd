% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desc_bivarie.R
\name{multi_quanti}
\alias{multi_quanti}
\title{multi_quanti}
\usage{
multi_quanti(
  data = NULL,
  var_princ,
  ...,
  moy = TRUE,
  test.diffmoy = TRUE,
  sd = TRUE,
  ic = TRUE,
  ic_seuil = 0.05,
  nb = 2,
  med = TRUE,
  quant = 4,
  minmax = TRUE,
  eff = TRUE,
  eff_na = FALSE,
  freq = TRUE,
  signif = NULL,
  pond = NULL,
  norm_pond = FALSE,
  design = NULL,
  msg = FALSE,
  NR = FALSE
)
}
\arguments{
\item{data}{base de données. Pas nécessaire s'il y a un survey-design réalisé.}

\item{var_princ}{variable principale (quantitative, en colonnes)}

\item{...}{variables catégorielles à croiser avec la variable principale (en lignes)}

\item{moy}{TRUE par défaut. Moyenne par modalité.}

\item{test.diffmoy}{\code{TRUE} par défaut. Réalise un test global d'égalité des moyennes entre les modalités, tenant compte du design avec \code{survey::svyglm()} et \code{survey::regTermTest()}.
Ce test repose sur une approximation asymptotique et doit être interprété avec prudence lorsque les effectifs ou le nombre de grappes sont faibles, ou lorsque la distribution comporte des valeurs extrêmes.}

\item{sd}{TRUE par défaut. Écart-type par modalité.}

\item{ic}{TRUE par défaut. Intervalle de confiance de la moyenne par modalité. N'apparait pas si moy = FALSE}

\item{ic_seuil}{risque de première espèce pour l'intervalle de confiance des moyennes.}

\item{nb}{nombre de décimales pour la moyenne, l'écart-type et l'intervalle de confiance.}

\item{med}{TRUE par défaut. Médiane par modalité.}

\item{quant}{4 par défaut. Nombre de quantiles. Si la médiane est sélectionnée, elle sera ajoutée si besoin. Prend 1 ou moins si pas de quantiles souhaités.}

\item{minmax}{TRUE par défaut. Minimum et maximum par modalité.}

\item{eff}{\code{TRUE} par défaut. Effectif pondéré des réponses valides à la variable quantitative par modalité.}

\item{eff_na}{\code{FALSE} par défaut. Effectif pondéré des non-réponses à la variable quantitative par modalité.}

\item{freq}{\code{TRUE} par défaut. Fréquence pondérée des réponses valides à la variable quantitative par modalité.}

\item{signif}{NULL par défaut, prend les valeurs 'etoiles' ou 'seuils'. Permet d'afficher les résultats des tests avec des étoiles ou avec des seuils à la place de la p.value en clair.}

\item{pond}{variable de pondération, le cas échéant}

\item{norm_pond}{\code{FALSE} par défaut. Normalise les poids utilisés pour les effectifs affichés afin que leur somme corresponde au nombre d'observations conservées.
Cette normalisation n'affecte pas le design utilisé pour les moyennes, intervalles de confiance, quantiles et tests.}

\item{design}{survey-design si déjà créé.}

\item{msg}{FALSE par défaut. Envoie un message pour chaque variable terminée : utile si bug inexpliqué.}

\item{NR}{FALSE par défaut. Garde les non-réponses des variables catégorielles.}
}
\value{
Un tibble avec en colonne les indicateurs synthétiques de la variable d'intérêt selon les modalités des variables catégorielles choisies.
}
\description{
Permet d'obtenir un tableau détaillant une variable quantitative en fonction d'une liste de variables catégorielles.
}
\author{
Julio Ricardo Davalos
}
